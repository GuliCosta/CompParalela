No código há tanto a versão serial quanto a versão paralela, o código da um print mostrando o tempo de execução do código em versão paralela e em versão serial e no final aparece o speedup entre as duas, no caso do código foi utilizado uma matriz de tamanho 200 e 2threads

